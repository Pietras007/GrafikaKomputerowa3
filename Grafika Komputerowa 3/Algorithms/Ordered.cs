using Grafika_Komputerowa_3.Constans;
using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Grafika_Komputerowa_3.Algorithms
{
    public static class Ordered
    {
        public static Color[,] ComputeAlgorithmVersion1(Color[,] currentImage, int Kr, int Kg, int Kb)
        {
            Color[,] image = new Color[CONST.bitmapWidth, CONST.bitmapHeight];
           

            return image;
        }

        public static Color[,] ComputeAlgorithmVersion2(Color[,] currentImage, int Kr, int Kg, int Kb)
        {
            Color[,] image = new Color[CONST.bitmapWidth, CONST.bitmapHeight];
            

            return image;
        }
    }
}
